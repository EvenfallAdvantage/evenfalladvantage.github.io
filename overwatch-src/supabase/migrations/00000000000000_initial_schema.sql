-- This file is a placeholder. The actual schema is applied by running
-- the migration scripts in ../prisma/ against the local Supabase instance.
-- 
-- To set up the local staging database:
-- 1. npx supabase start
-- 2. Run each prisma/*.sql file in order via the Supabase Studio SQL editor
--    (accessible at http://localhost:54323)
-- 
-- Or use the staging setup script:
-- npm run staging:setup

SELECT 'Migration placeholder — see prisma/ directory for actual schema' AS info;
